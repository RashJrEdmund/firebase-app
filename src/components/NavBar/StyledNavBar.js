import styled from '@emotion/styled';

const StyledNavBar = styled.div`
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: fit-content:
    padding: 10px;
`;

export default StyledNavBar;
